<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="02"
    android:versionName="1.0.1"
    android:installLocation="auto"
    package="com.lwazir.coronatracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/corona_app_icon"
        android:label= "@string/app_name"
        android:logo="@mipmap/corona_app_icon"
        android:roundIcon="@mipmap/corona_app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/MyToolbarTheme">
        <activity android:name=".Main_Tab_Activity"
            android:label="@string/app_name"
            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>